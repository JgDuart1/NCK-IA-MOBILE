{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": ".",
    "moduleResolution": "bundler",
    "customConditions": ["react-native", "default", "types", "import", "require"],
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
